query GetOrderList($filter: FilterQueryInput!) {
  billingViewOrderList(filter: $filter) {
  orders {
      status
      orderCode
      vendorCode
      orderUpdatedAt
      orderPlacedAt
      isBillable
      entityId
    },
    pagingKey {
      flow
      orderCode
      orderPlacedAt
    }
  }
}
